"use client";

import type { PropsWithChildren } from "react";

/** Route provider stub - react-aria-components removed. */
export function RouteProvider({ children }: PropsWithChildren) {
  return <>{children}</>;
}
